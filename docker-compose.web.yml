services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: mingai-web:latest
    container_name: mingai-web
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${WEB_PORT:-3000}:3000"
