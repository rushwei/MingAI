// divination/ barrel export
// 由于各模块间存在同名导出（如 BAN_HE_TABLE），不使用 export * 聚合。
// 请直接从具体模块导入，例如：
//   import { ... } from '@/lib/divination/bazi'
//   import { ... } from '@/lib/divination/fortune'
// 旧路径 @/lib/bazi 等仍可用（通过 re-export 兼容）。
